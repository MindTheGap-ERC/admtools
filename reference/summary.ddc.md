# summary of depth-depth curve

displays some summary numbers of a depth-depth curve (`ddc` object)

## Usage

``` r
# S3 method for class 'ddc'
summary(object, ...)
```

## Arguments

- object:

  depth-depth curve (`ddc`)

- ...:

  other variables, currently ignored

## Value

invisible NULL
